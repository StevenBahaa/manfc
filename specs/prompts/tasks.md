Break the migration into very small safe tasks.

Rules:

- each task must touch minimal files
- each task must be independently testable
- no large refactors
- no UI redesign
- start with infrastructure only
- migrate invoice feature last
